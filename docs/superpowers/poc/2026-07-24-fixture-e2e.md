# Direct Processing Fixture Verification

Date: 2026-07-24

Status: automated core verification passed; the previous Campaign-based build
confirmed the normal Chrome API and content-script boundary on 2026-07-25. The
new direct records flow is pending one normal-profile Chrome acceptance run.

## Decision

Continue with the normal Chrome profile for extension acceptance. The automated
Chrome launcher still cannot validate the extension because it starts Chrome
with extensions disabled, but the user confirmed LinkMaster connection and
content-script page inspection in their normal Chrome profile.

No real external page received a comment during this verification.

## Artifacts

- Unpacked extension:
  `/Users/coderlim/Projects/link-booster-extension/.worktrees/auto-backlink-campaign-executor/build/chrome-mv3-prod`
- ZIP:
  `/Users/coderlim/Projects/link-booster-extension/.worktrees/auto-backlink-campaign-executor/build/chrome-mv3-prod.zip`
- ZIP SHA-256:
  `687074972925aa4567697b81662c7df0744ff4b8187ccea4e9b843e5100cf0be`

The packaged manifest is MV3 version `0.0.3`, declares a side panel and options
page, and includes storage, tabs, scripting, and side-panel permissions.

## Automated Evidence

Commands:

```bash
pnpm test
pnpm build
pnpm package
```

Results:

| Check | Result |
| --- | --- |
| Link Booster unit tests | 82 passed, 0 failed |
| LinkMaster unit tests | 149 passed, 0 failed |
| Production build | passed |
| ZIP packaging | passed, 0.44 MB |
| Direct API domain/store/routes | passed in isolated tests |
| TypeScript | passed |

Fixture behaviors covered in the real extension modules:

| Scenario | Automated result |
| --- | --- |
| Website field / username link | detected and filled |
| Legacy field names and rich-text editors | detected and filled |
| Detected comment form | smooth-scrolls to the form once during inspection |
| Proven body HTML link | detected and formatted |
| Markdown, BBCode, and plain URL evidence | detected |
| No proven link placement | rejected |
| No comment form | unrelated forms ignored |
| Login required | detected |
| Visible CAPTCHA | detected |
| Passive reCAPTCHA v3 resources | ignored; normal form flow continues |
| Moderation | detected and classified |
| Published comment | requires adjacent exact target link |
| Explicit rejection | classified |
| Silent rejection | classified |
| Single automation tab | created once and reused |
| Terminal result | added to the local execution list before the next backlink |
| Synchronization failure | retains rows and offers Retry Sync |
| Reviewed metadata | original and proposed values remain visible until Sync |
| Tab navigation | waits for complete load |
| Closed automation tab | fails the pending navigation |
| GET network error | retries once |
| POST result network error | never automatically retries |
| Submit control | only detected comment-form submit is clicked |

Comment-generation tests also verify 1-3 sentences, page-grounded details,
language agreement, exact body-link formatting, no target mention in username
link mode, one regeneration, and failure after the second invalid generation.

## Browser Acceptance Status

Confirmed in the user's normal Chrome profile on 2026-07-25:

- options page saved the LinkMaster base URL and token;
- Test Connection reached the local API;
- the side panel loaded and executed the previous active Campaign;
- content-script inspection crossed the real extension boundary.

Still pending after reloading the direct-processing build:

- Automatic lists and preselects the CSV Viewer target;
- Start opens the first unrecorded non-root backlink without a Campaign;
- the known Saporta Report WordPress form reaches `awaiting_review` and is
  filled;
- one visible automation tab is reused across multiple fillable Items;
- the side panel pauses at `awaiting_review`;
- only a user click can submit;
- every opened Item appears in the execution list with visible status;
- terminal results do not appear in `records.json` until Sync is clicked;
- pause, skip, retry sync, and stop controls work visually;
- compact and narrow side-panel layouts do not overlap.

The first real diagnostic run exposed two form-layer issues: the detector had
narrower field coverage than the original manual filler, and passive reCAPTCHA
v3 assets were treated as a blocking challenge. The shared detector now reuses
the original broad field coverage while still excluding contact, search, and
newsletter forms. Passive v3 assets no longer block filling; visible challenges
still require manual handling or skipping.

## Manual Fixture Matrix

Use local or disposable pages only for the first Chrome run:

| Fixture | Expected result |
| --- | --- |
| Author name + email + Website URL + comment | `UserName Link`, no body target |
| Comment editor with proven HTML link examples | `HTML Link`, exact body target |
| Comment editor with Markdown examples | `Markdown Link`, exact body target |
| Comment editor with BBCode examples | `BBCode Link`, exact body target |
| Legacy `user_name` / `mail` / `site` / `message` fields | detected and filled |
| Quill/Summernote/Froala/CKEditor/contenteditable editor | detected and filled |
| Contact/search/newsletter form only | `cannot_submit` |
| Login prompt | `cannot_submit` |
| Passive reCAPTCHA v3 resources only | continue to `awaiting_review` |
| Visible CAPTCHA challenge | pause/skip without automatic solving |
| Moderation success message | `pending_moderation` |
| Explicit error message | `explicit_reject` |
| No success/error and comment absent | `silent_reject` |

For submission fixtures, inspect the filled fields first and click Submit and
Continue once. Do not test automatic submission because the POC intentionally
does not implement it.

## Exit Criteria

This phase becomes a full fixture E2E pass only after the manual Chrome checks
above are recorded with expected and actual results. Until then:

- code/build/package status: passed;
- API and DOM module integration status: passed;
- real Chrome extension boundary: confirmed;
- external direct execution: normal-profile acceptance pending.
